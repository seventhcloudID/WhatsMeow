# Port HTTP server
PORT=8081

# API key untuk autentikasi (wajib di production)
API_KEY=your-secret-api-key

# Path database SQLite untuk session WhatsApp
DB_PATH=data/session.db

# URL webhook untuk menerima pesan masuk (opsional)
WEBHOOK_URL=

# Level log: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO
